import { Box, Typography, Card, CardContent } from "@mui/material";

const Body = () => {
  return (
    <Box>
      <Typography variant="h4">Dashboard body</Typography>
    </Box>
  );
};

export default Body;
